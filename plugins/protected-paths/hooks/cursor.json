{
    "version": 1,
    "hooks": {
        "preToolUse": [
            {
                "matcher": "Shell|Read|Write|Grep",
                "command": "node ${PLUGIN_ROOT}/scripts/protected-paths.js"
            }
        ]
    }
}
